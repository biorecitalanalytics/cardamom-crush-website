Add menu item photos to this folder.

Recommended file names:

- Use lowercase letters, numbers, and hyphens.
- Use `.jpg`, `.png`, or `.webp`.
- Example: `masala-chai.jpg`

Then reference the file from `editable-data.js`:

```json
"image": "/assets/menu/masala-chai.jpg"
```
